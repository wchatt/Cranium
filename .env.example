# ── Required: Slack Tokens ────────────────────────────────────────────────────
# From your Slack app (see README for setup instructions)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# ── Claude Model ──────────────────────────────────────────────────────────────
# Which model to use for interactive messages (default: opus)
# Options: opus, sonnet, haiku
CLAUDE_MODEL=opus

# ── Claude Authentication ─────────────────────────────────────────────────────
# On a headless server, run: claude setup-token
# Then copy the token value here so it persists across sessions and systemd restarts.
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-xxxxx

# ── Claude Binary ────────────────────────────────────────────────────────────
# Path to Claude CLI binary (defaults to "claude" on PATH)
# CLAUDE_BIN=/usr/local/bin/claude

# ── Cron Jobs ─────────────────────────────────────────────────────────────────
# API key for scheduled jobs (keeps cron off your Max quota)
# Get one at https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Slack channel ID for cron job notifications
# Find this: open a DM with your bot → click channel name → copy Channel ID
# SLACK_DM_CHANNEL=D0123456789

# ── Voice Interface ───────────────────────────────────────────────────────────
# VOICE_PORT=3100
# VOICE_BIND_HOST=127.0.0.1

# ── Optional: MCP Tools ──────────────────────────────────────────────────────
# If you have an mcp-config.json for Notion, Google, etc., it's auto-detected.
# No env var needed — just create the file in the repo root.
